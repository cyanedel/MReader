<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="mreader.Views.Pages.ImageViewPage"
             Title="Reader">
    <Grid RowDefinitions="Auto,*">
        <Label x:Name="TitleLabel" Padding="16,12" FontSize="18" FontAttributes="Bold" LineBreakMode="TailTruncation" />
        <CollectionView x:Name="Pages" Grid.Row="1" SelectionMode="None">
            <CollectionView.ItemsLayout><LinearItemsLayout Orientation="Vertical" ItemSpacing="8" /></CollectionView.ItemsLayout>
            <CollectionView.EmptyView><Label x:Name="EmptyLabel" Padding="24" HorizontalTextAlignment="Center" Text="No readable images were found." /></CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <VerticalStackLayout Padding="0,0,0,8" Spacing="4">
                        <Image Source="{Binding Source}" Aspect="AspectFit" HorizontalOptions="Fill" />
                        <Label Text="{Binding Name}" FontSize="11" TextColor="{StaticResource Gray500}" HorizontalTextAlignment="Center" />
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
