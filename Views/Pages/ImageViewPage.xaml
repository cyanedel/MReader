<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="mreader.Views.Pages.ImageViewPage"
             Title="ImageView">
	<ScrollView>
		<VerticalStackLayout>
			<VerticalStackLayout>
				<Label 
							Text="Welcome to .NET MAUI! View Page"
							VerticalOptions="Center" 
							HorizontalOptions="Center" />
			</VerticalStackLayout>
			<CollectionView
					ItemsSource="{Binding DirPaths}"
					HorizontalOptions="Center"
					VerticalOptions="Fill">
				<CollectionView.ItemsLayout>
					<GridItemsLayout Orientation="Vertical"
                             Span="4"
                             HorizontalItemSpacing="10"
                             VerticalItemSpacing="10"/>
				</CollectionView.ItemsLayout>
				<CollectionView.ItemTemplate>
					<DataTemplate>
						<Border StrokeThickness="0">
						<VerticalStackLayout>
							<Image Source="{Binding PathImage}"
								 WidthRequest="160"
								 HeightRequest="200"
								 Aspect="AspectFill"/>
							<Label Text="{Binding PathName}" HorizontalOptions="Center" />
						</VerticalStackLayout>
						</Border>
					</DataTemplate>
				</CollectionView.ItemTemplate>
			</CollectionView>
			<VerticalStackLayout
					BindableLayout.ItemsSource="{Binding FilePaths}" >
				<BindableLayout.ItemTemplate>
					<DataTemplate>
						<Label Text="{Binding}" />
					</DataTemplate>
				</BindableLayout.ItemTemplate>
			</VerticalStackLayout>
		</VerticalStackLayout>
	</ScrollView>
</ContentPage>